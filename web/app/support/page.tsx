import { PageHero } from "@/components/page-hero";
import { StatusChip } from "@/components/status-chip";
export default function SupportPage() { return <div className="section-shell page-stack"><PageHero eyebrow="SUPPORT" title="Report problems without exposing secrets." description="The full reporting, tracking and notification backend is a separate security-sensitive subsystem. This page currently provides safe support boundaries." /><section className="card-grid"><article className="info-card"><StatusChip status="Planned" /><h2>Report problem</h2><p>A structured report flow will collect only safe diagnostics and issue context.</p></article><article className="info-card"><StatusChip status="Planned" /><h2>Track report</h2><p>Report IDs and status tracking will be enabled when the backend lands.</p></article><article className="info-card"><StatusChip status="In Development" /><h2>Known issues</h2><p>Until the support system exists, use repository issues for non-sensitive reproducible defects.</p></article><article className="info-card"><StatusChip status="Available" /><h2>Security</h2><p>Sensitive vulnerabilities should follow the repository SECURITY.md process, not public issue content.</p><a className="text-link" href="https://github.com/sudharma-networks/sudharma/blob/main/SECURITY.md">Security guidance ↗</a></article></section><section className="notice danger"><strong>Never send secrets.</strong><p>No support workflow should request seed phrases, private keys, AWS credentials, privileged RPC credentials or authentication tokens.</p></section></div>; }
