# operations

Operator-facing configuration and structured logging for Sudharma production nodes.

The package deliberately contains no wallet/private-key material. Node configuration is suitable for source-controlled templates only when it contains public infrastructure settings; machine-specific deployment files may still contain private hostnames and should be handled according to the operator's infrastructure policy.
